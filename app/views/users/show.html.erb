<h3>This is your home page.</h3>
<div class="col-xs-4">
  <div>
    Your have finished 0 games
  </div>
  <%= link_to "Start new game", games_path(id: current_user.id), :method => :post, data: { no_turbolink: true } %>
</div>
<div class="col-xs-8">
  <h4>Your active games:</h4>
  <ul>
    <% if @user.games.count == 0 %>
      <h5>There are no games yet!</h5>
    <%else%>
        <% @user.games.each do |game|%>
          <li><%= link_to "game №#{game.id}", game_path(game), data: { no_turbolink: true }%></li>
        <%end%>
    <%end%>
  </ul>
</div>